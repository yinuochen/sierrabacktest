[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "backtest"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy", "matplotlib"]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "backtest._engine"
